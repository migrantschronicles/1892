using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/**
 * Is added to all npc characters.
 * Currently empty, but exists in case data needs to be added.
 * Most npcs can only idle or talk (during dialogs).
 */
public class NPCAnimationController : IAnimationController
{
    
}
